# 🚀 Frontend: ระบบจัดการตารางสอบ

โปรเจกต์นี้คือส่วนหน้า (Frontend) ของ **ระบบจัดการตารางสอบ** ซึ่งได้รับการพัฒนาขึ้นโดยมุ่งเน้นที่ความรวดเร็ว ประสิทธิภาพ และประสบการณ์ผู้ใช้ที่ยอดเยี่ยม ด้วยการใช้เทคโนโลยี Frontend ที่ทันสมัยที่สุด

---

## ✨ คุณสมบัติหลัก

-   **React**: ไลบรารี JavaScript ยอดนิยมสำหรับการสร้างส่วนติดต่อผู้ใช้ (User Interface) ที่มีประสิทธิภาพและสามารถนำกลับมาใช้ใหม่ได้ ทำให้การพัฒนา UI ซับซ้อนเป็นเรื่องง่าย
-   **TypeScript**: เพิ่มความสามารถในการตรวจสอบชนิดข้อมูล (Type-checking) ในระหว่างการพัฒนา ช่วยลดข้อผิดพลาด ทำให้โค้ดมีความน่าเชื่อถือและบำรุงรักษาง่ายขึ้น
-   **Vite**: เครื่องมือ Build Tool ที่รวดเร็วและน้ำหนักเบา มอบประสบการณ์การพัฒนาที่เหนือกว่าด้วย Hot Module Replacement (HMR) ที่รวดเร็ว และการ Build สำหรับ Production ที่มีประสิทธิภาพ
-   **Tailwind CSS**: Utility-first CSS framework ที่ช่วยให้การออกแบบและสร้าง UI เป็นไปได้อย่างรวดเร็วและยืดหยุ่น โดยไม่ต้องเขียน CSS จากศูนย์
-   **Axios**: HTTP client ที่มีประสิทธิภาพสำหรับจัดการการเรียก API ไปยัง Backend ทำให้การรับส่งข้อมูลกับเซิร์ฟเวอร์เป็นเรื่องง่าย
-   **Zustand**: โซลูชันการจัดการสถานะ (State Management) ที่เรียบง่าย รวดเร็ว และสามารถปรับขนาดได้ เหมาะสำหรับแอปพลิเคชัน React
-   **React Router DOM**: ไลบรารีสำหรับการจัดการเส้นทาง (Routing) ในแอปพลิเคชัน React ทำให้สามารถสร้าง Single Page Application (SPA) ที่มี URL ที่จัดการได้

---

## 📦 การเริ่มต้น (Getting Started)

### ข้อกำหนดเบื้องต้น





### ⬇️ ติดตั้ง Dependencies

นำทางไปยังไดเรกทอรี `frontend` และติดตั้งแพ็กเกจที่จำเป็นทั้งหมด:

```bash
bun install
```

---

## 💻 การพัฒนา (Development)

### 🚀 รัน Development Server

เมื่อติดตั้ง Dependencies เรียบร้อยแล้ว สามารถเริ่มเซิร์ฟเวอร์สำหรับการพัฒนาได้:

```bash
bun run dev
```

เซิร์ฟเวอร์จะรันบน `http://localhost:5173` โดยแอปพลิเคชันจะอัปเดตอัตโนมัติเมื่อมีการเปลี่ยนแปลงโค้ด (Hot Module Replacement)

---

## 🛠 การ Build สำหรับ Production

หากต้องการ Build โปรเจกต์สำหรับ Production ให้ใช้คำสั่ง:

```bash
bun run build
```

ไฟล์ที่ถูก Build จะอยู่ในไดเรกทอรี `dist/` ซึ่งพร้อมสำหรับการ Deploy

---

## 📂 โครงสร้างโปรเจกต์

โครงสร้างไดเรกทอรีหลักของโปรเจกต์ Frontend:

```
frontend/
├───src/
│   ├───main.tsx             # จุดเริ่มต้นของแอปพลิเคชัน React
│   ├───App.tsx              # คอมโพเนนต์หลักของแอปพลิเคชัน
│   ├───components/          # คอมโพเนนต์ UI ที่นำกลับมาใช้ใหม่ได้ (เช่น AdminLayout, AuthLayout, forms, ui)
│   ├───contexts/            # สำหรับจัดการ Global State ของแอปพลิเคชัน (เช่น AuthContext)
│   ├───pages/               # หน้าต่างๆ ของแอปพลิเคชัน (เช่น LoginPage, UserDashboard, AdminDashboard, NotFoundPage)
│   ├───services/            # สำหรับการเรียก API และ Logic ที่เกี่ยวข้องกับ Backend (เช่น api.ts)
│   └───types/               # การกำหนด Type ของข้อมูลด้วย TypeScript สำหรับ Entities ต่างๆ (เช่น auth, user, course)
├───public/                  # ไฟล์ Static Assets ที่จะถูกคัดลอกไปยัง Build Output โดยตรง
├───index.html               # ไฟล์ HTML หลัก
├───package.json             # ข้อมูลโปรเจกต์และ Dependencies
├───tsconfig.json            # การตั้งค่า TypeScript
└───vite.config.ts           # การตั้งค่า Vite Build Tool
```
