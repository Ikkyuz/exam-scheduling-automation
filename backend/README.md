# 🚀 Backend: ระบบจัดการตารางสอบ (Elysia + Bun + Prisma + Docker)

โปรเจกต์นี้คือส่วนหลังบ้าน (Backend) ของระบบจัดการตารางสอบ สร้างขึ้นด้วยเทคโนโลยีที่ทันสมัยเพื่อประสิทธิภาพสูงสุดและความยืดหยุ่นในการพัฒนาและดูแลรักษา

---

## ✨ คุณสมบัติหลัก

-   **Elysia Framework**: Web framework ที่รวดเร็วและใช้ TypeScript เป็นหลัก เหมาะสำหรับการสร้าง API ที่มีประสิทธิภาพ
-   **Bun Runtime**: รันไทม์ JavaScript แบบ All-in-one ที่ปฏิวัติวงการ มาพร้อม Bundler, Transpiler และ Package Manager ในตัว ช่วยให้การพัฒนาและการรันแอปพลิเคชันเร็วขึ้นอย่างเห็นได้ชัด
-   **Prisma ORM**: Object-Relational Mapper (ORM) สมัยใหม่ที่ช่วยให้การทำงานกับฐานข้อมูลเป็นเรื่องง่ายขึ้น รองรับ Type-safe database access, Schema Migrations และรองรับทั้ง SQL & NoSQL Database (MySQL, PostgreSQL, MongoDB)
-   **Docker & Docker Compose**: เพิ่มความสะดวกในการ Deploy และสร้างสภาพแวดล้อมการพัฒนาที่สอดคล้องกันทุกแพลตฟอร์ม

---

## 📦 การเริ่มต้น (Getting Started)

### ข้อกำหนดเบื้องต้น

-   **Bun**: รันไทม์ JavaScript (แนะนำสำหรับการพัฒนา)
-   **Docker & Docker Compose**: สำหรับการจัดการ Database และ Services ต่างๆ ในสภาพแวดล้อมที่แยกจากกัน

### ⬇️ ติดตั้ง Dependencies

นำทางไปยังไดเรกทอรี `backend` และติดตั้งแพ็กเกจที่จำเป็นทั้งหมด:

```bash
cd backend
bun install
```

### ⚙️ การตั้งค่าสภาพแวดล้อม (.env)

สร้างไฟล์ `.env` ที่ไดเรกทอรี `backend` และกำหนดค่าเชื่อมต่อฐานข้อมูลของคุณ ตัวอย่างเช่น:

**สำหรับ MySQL:**
```env
DATABASE_URL="mysql://root:mysecretpassword@localhost:3306/your_database_name"
```

**สำหรับ PostgreSQL (เมื่อใช้ Docker Compose):**
```env
DATABASE_URL="postgresql://user:password@db:5432/your_database_name"
```
*(โปรดแทนที่ `user`, `password`, `your_database_name` ด้วยค่าจริง)*

---

## 💻 การพัฒนา (Development)

### 🚀 รัน Development Server

เมื่อติดตั้ง Dependencies และตั้งค่า `.env` เรียบร้อยแล้ว สามารถเริ่มเซิร์ฟเวอร์สำหรับการพัฒนาได้:

```bash
bun run dev
```

เซิร์ฟเวอร์จะรันบน `http://localhost:3000` โดยคุณสามารถเข้าถึงเอกสารประกอบ API (Swagger UI) ได้ที่ 👉 `http://localhost:3000/docs`

---

## 🗃️ Prisma ORM & การจัดการฐานข้อมูล

โปรเจกต์นี้ใช้ Prisma ORM ในการจัดการ Schema, การย้ายฐานข้อมูล (Migrations) และการสร้าง Type สำหรับการทำงานกับฐานข้อมูลอย่างปลอดภัย

### 📝 คำสั่ง Prisma ที่สำคัญ

-   **Initialize Prisma**:
    *   เริ่มต้น Prisma ในโปรเจกต์ (เลือก Provider ตามฐานข้อมูลที่คุณใช้):
        ```sh
        bun x prisma@latest init --datasource-provider=mysql
        # หรือ
        bun x prisma@latest init --datasource-provider=postgresql
        ```
-   **Generate Prisma Client**:
    *   สร้าง Prisma Client เพื่อให้สามารถโต้ตอบกับฐานข้อมูลในโค้ดของคุณ:
        ```sh
        bun x prisma generate
        ```
    *   หากต้องการระบุ Schema เฉพาะ:
        ```sh
        bun x prisma generate --schema=prisma/schema.mysql.prisma
        bun x prisma generate --schema=prisma/schema.postgres.prisma
        ```
-   **Sync Schema กับ Database**:
    *   ปรับใช้การเปลี่ยนแปลง Schema ไปยังฐานข้อมูลของคุณโดยตรง (เหมาะสำหรับการพัฒนา):
        ```sh
        bun x prisma db push
        ```
-   **Reset Database**:
    *   รีเซ็ตฐานข้อมูลทั้งหมด (ลบข้อมูลและ Schema แล้วสร้างใหม่):
        ```sh
        bun x prisma migrate reset
        ```
-   **เปิด Prisma Studio**:
    *   เปิด GUI สำหรับการดูและจัดการข้อมูลในฐานข้อมูลของคุณ:
        ```sh
        bun x prisma studio
        ```

---

## 🐳 Docker Workflow

การใช้ Docker ช่วยให้การตั้งค่าสภาพแวดล้อม Database และ Caddy (หากมี) เป็นเรื่องง่ายและสอดคล้องกัน

### 🏗️ Build Docker Image

สร้าง Docker image สำหรับแอปพลิเคชัน Backend ของคุณ:

```sh
docker build -t exam-scheduling .
```

หรือใช้ Docker Compose เพื่อ Build Services ทั้งหมด:

```sh
docker compose build
```

### 🚀 รัน Services ด้วย Docker Compose

เริ่มต้น Services ทั้งหมดที่กำหนดไว้ใน `docker-compose.yaml` (เช่น Database, Backend app, Caddy):

```sh
docker compose up -d
```

#### รัน Services แยกกัน (หากต้องการ)

-   **เฉพาะ Database Service**:
    ```sh
    docker compose -f compose.database.yaml up -d
    ```
-   **เฉพาะ Caddy Service**:
    ```sh
    docker compose -f compose.caddy.yaml up -d
    ```

### 🛑 หยุดและลบ Services ของ Docker Compose

เมื่อต้องการหยุดการทำงานและลบ Services ที่รันด้วย Docker Compose:

```sh
docker compose down -v
```

---

## 📂 โครงสร้างโปรเจกต์

โครงสร้างไดเรกทอรีหลักของโปรเจกต์ Backend:

```
backend/
├───src/
│   ├───index.ts             # ไฟล์เริ่มต้นของแอปพลิเคชัน
│   ├───features/            # โมดูลของแต่ละคุณสมบัติ (เช่น auth, class, user)
│   ├───providers/           # การจัดการ Providers (เช่น database)
│   └───shared/              # โค้ดที่ใช้ร่วมกัน (เช่น middleware, utils)
├───prisma/
│   └───schema.prisma        # ไฟล์กำหนด Database Schema
├───.env.example             # ไฟล์ตัวอย่างสำหรับการตั้งค่าสภาพแวดล้อม
├───package.json             # ข้อมูลโปรเจกต์และ Dependencies
├───tsconfig.json            # การตั้งค่า TypeScript
└───Dockerfile               # Dockerfile สำหรับการสร้าง Docker Image
```
